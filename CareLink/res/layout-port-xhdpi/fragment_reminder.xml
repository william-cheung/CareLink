<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/default_background"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/add_record_panel_bg_layer_list" >

        <Button
            android:id="@+id/button_addReminder"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_margin="10dp"
            android:background="@drawable/button_bg_selector_add_record"
            android:text="@string/text_add_reminder"
            android:textSize="18sp" />
    </LinearLayout>
    
    <ListView
        android:id="@+id/listView_reminders"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@null"
        android:listSelector="@android:color/transparent" >
    </ListView>

</LinearLayout>